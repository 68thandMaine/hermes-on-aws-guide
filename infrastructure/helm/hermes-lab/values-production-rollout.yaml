# Chapter 40 — production rollout settings for hermes-lab
# helm upgrade hermes-lab ... -f values.yaml -f values-production-rollout.yaml

api:
  replicaCount: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
