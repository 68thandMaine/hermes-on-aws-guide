# Chapter 32 — ServiceMonitor pattern for application /metrics (Hermes bridge)
# nginx-demo does not expose /metrics by default — this is the pattern for Part VI Hermes API.
# Requires kube-prometheus-stack with prometheusOperator enabled.
# Uncomment and adapt when Hermes (or nginx-exporter sidecar) exposes port 9090 /metrics.
#
# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   name: hermes-api
#   namespace: default
#   labels:
#     release: monitoring
# spec:
#   selector:
#     matchLabels:
#       app: hermes-api
#   endpoints:
#     - port: metrics
#       path: /metrics
#       interval: 30s
