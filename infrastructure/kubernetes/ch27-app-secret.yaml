# Chapter 27 — demo secret (NOT production-safe — lab only)
# echo -n 'hello_world' | base64  →  aGVsbG9fd29ybGQ=
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  labels:
    app: config-demo
type: Opaque
stringData:
  API_KEY: hello_world
